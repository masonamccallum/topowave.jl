module topowave
using Plots

function main()
   return 0 
end
end # module

"""
rd = StartUpDG.RefElemData(Tri(),3);
md = MeshData(uniform_mesh(Tri(),90)...,rd);
mp = MeshPlotter(rd,md);
plot(mp)
"""

x= 1